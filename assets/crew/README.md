# Starter crew sprites

The 8-by-8 `MRdiabetes` and `MrDpwnSyndrome` sprites were supplied by the project owner for the starter crew and imported with normalized snake-case filenames. Their pixel content is unchanged from the supplied PNG files.
