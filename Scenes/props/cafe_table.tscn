[gd_scene load_steps=2 format=3]

[ext_resource type="Texture2D" path="res://Assets/Props/cafe_table.png" id="1_table"]

[node name="CafeTable" type="Sprite2D"]
editor_description = "Cafe table. Shares the stool's scale so the two keep the proportions they were drawn at; the offset stands it on the node's origin."
texture = ExtResource("1_table")
offset = Vector2(0, -293.5)
scale = Vector2(0.085, 0.085)
